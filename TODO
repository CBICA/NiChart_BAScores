1) Multiclass classification is not ready yet, i have to change a lot for it to work, but it will be done once i have to train a model with multiclass data
    update: Multiclass classification is somewhat ready, im not quite sure if i made a mistake somewhere.

2) Update documentation with the updated parameters(mode, num_classes)

3) Update new inference in PairwiseDataloader as well(see what i changed in SingleSubjectDataloader). Inference should return mrids as well so that we can add them into the output csv.